# Streamlit version

The full Streamlit app is available in the original sandbox.

To run a web version of the estimator:

```bash
streamlit run app/streamlit_app.py
```

(The full `streamlit_app.py` source follows the same architecture as the Tauri sidecar and desktop app.)
